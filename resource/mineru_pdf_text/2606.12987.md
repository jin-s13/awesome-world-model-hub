# Difusion Transformer World-Action Model for AV Scene Prediction

Ruslan Sharifullin Stanford University rshar@stanford.edu

Benjamin Jiang Stanford University benjiang@stanford.edu

Kai Xi Chew Stanford University kxchew@stanford.edu

## Abstract

We study compact, action-conditioned world models for autonomous driving. Given the current front-camera latent and a sequence of ego-actions, our system predicts future scene latents that a frozen decoder renders to 256×256 frames up to 8 seconds ahead. We evaluate all models on 150 held-out nuScenes driving scenes across diverse urban conditions in Boston and Singapore. We first benchmark where to predict: across six frozen visual encoders spanning four representation families, V-JEPA2 with temporal context reduces steering RMSE by 40% relative to the best singleframe encoder. We then build a latent Difusion Transformer (DiT) and, through a controlled hypothesis-driven diagnosis, identify the four ingredients it needs: spatial tokens, the <sup>??</sup> prediction objective, residual anchoring, and sampling matched to target uncertainty. Moving to a Stable-Difusion-VAE encode-predict-decode pipeline, we surface a central tension. Standard distortion metrics (cosine similarity, SSIM) favor the blurry regression mean, masking the fact that the difusion model is far closer to the real frame distribution. Measuring with Inception-based FID and KID reveals a clean perception-distortion frontier: the difusion model attains KID 0<sup>.</sup>078 versus 0<sup>.</sup>375 for direct regression (4<sup>.</sup>8× better), and a deployable train-derived calibration makes this practical without access to test-time ground truth. The model is genuinely action-controllable: steering monotonically drives predicted scene displacement (Spearman <sup>??</sup>=0<sup>.</sup>81) where the regression baseline is uncorrelated (<sup>??</sup>=−0<sup>.</sup>18). We diagnose limited single-pass temporal motion to a shared-present anchor and show that a compact 1.7 M-parameter “jump” world model recovers full groundtruth motion magnitude (1<sup>.</sup>02× GT) on held-out scenes, where single-pass models capture less than half. The result is a small, controllable AV world model with a realism frontier and a validated route to coarse temporal motion; higher appearance fidelity scales with capacity and data.

## 1. Introduction

World models for autonomous driving predict future scene representations conditioned on ego-actions, enabling planning and simulation without acting in the environment [12, 14, 23]. Yet two questions precede any such model: which latent space to predict in, and whether a generative transformer adds value over a deterministic regressor in that space.

Input and output. The input to our system is the current front-camera frame, encoded to a Stable-Difusion-VAE latent [19], together with a sequence of logged ego-actions (steering angle, acceleration). A latent Difusion Transformer (DiT) [16] world-action model outputs predicted future scene latents at up to 16 horizon steps (8 s at 2 Hz), which the frozen VAE decoder renders back to 256×256 frames (Figure 1).

We answer the first question with a systematic benchmark of six frozen encoders spanning supervised, self-supervised image, self-supervised video, and reconstruction families. V-JEPA2 [3] with temporal context dramatically outperforms all single-frame alternatives. We answer the second through a controlled, hypothesis-driven diagnosis that isolates when a DiT helps: the critical factors are the prediction objective (<sup>??</sup><sub>0</sub> vs. <sup>??</sup>), spatial structure, residual anchoring, and the match between sampling and target uncertainty.

The core of the paper is what happens in the productionstyle VAE encode-predict-decode pipeline, where appearance ambiguity under point losses becomes pronounced. A deterministic regressor wins every distortion metric by collapsing to a blurry conditional mean. Yet this is exactly the perception-distortion tradeof [5]: distribution metrics (FID/KID [9, 4]) reveal the difusion model is far closer to the real frame distribution. We characterize this frontier, make the difusion advantage deployable via a train-derived calibration, demonstrate action controllability and show a reparameterization that recovers forward-motion direction.

![](images/772b1e527d72b426b457bc629709d1d1a211ffa571ad78777cb48cbb6997b380.jpg)  
Figure 1: Single-pass architecture. A frozen SD-VAE encodes the present front-camera frame to a 32×32×4 latent grid; egoactions are embedded via learned Fourier features. The Anchored VAE DiT (4 blocks, adaLN-Zero conditioning) predicts future latent tokens as residuals from the present anchor. The frozen VAE decoder renders the predicted latents back to 256×256 frames. In difusion mode, the input is noised future latents; in direct (regression) mode, the present latent is replicated.

## Contributions.

1. A systematic 6-encoder benchmark for ego-action prediction on nuScenes (850 scenes), with among the first AV evaluations of V-JEPA2 (40% steering RMSE reduction from temporal context).

2. A controlled DiT diagnosis isolating objective (<sup>??</sup><sub>0</sub> recovers 88.5% of the gap), representation, anchoring, and sampling as the factors governing transformer utility in compact latents.

3. A perception-distortion characterization for AV latent world models: an empirical frontier, a 4<sup>.</sup>8× KID advantage for difusion, and a deployable train-derived calibration. We argue that AV world models should be evaluated with distribution metrics, not distortion alone.

4. Action controllability (<sup>??</sup>=0<sup>.</sup>81), together with a diagnosis-driven jump model that recovers forwardmotion direction, beating a 3×-larger baseline.

## 2. Related Work

AV world models. GAIA-1 [12] generates future driving video from text and actions via an autoregressive transformer. Cosmos [14] and MovieGen-style video priors [17] scale latent video generation to internet-scale data. Drive-Dreamer [25] and GenAD [26] couple trafic structure with difusion for driving-video synthesis. DIAMOND [1] applies difusion world dynamics to learn environment simulators for Atari game control. UniSim [24] builds interactive real-world simulators from diverse data sources. These systems typically operate in pixel or high-dimensional spatial-token latent spaces at large scale. Concurrent work by Shi et al. [20] demonstrates DiT world-action models at 5 B parameters with video priors; our study instead operates in the compact regime and asks which design factors and evaluation metrics matter, contributing controlled analysis rather than a scaled system.

Visual encoders for driving. DINOv2 [15] provides self-distilled image features; CLIP [18] learns visionlanguage aligned representations; V-JEPA and V-JEPA2 [2, 3] learn predictive video representations by masking and reconstructing latent features across time; VQGAN [8] produces reconstruction-oriented codebook tokens. Prior driving work typically fixes one encoder without systematic comparison; we evaluate across families and quantify the advantage of temporal video representations.

Difusion transformers and the perception-distortion tradeof. DiT [16] applies transformer blocks with adaptive layer normalization (adaLN-Zero) for class-conditioned image generation, demonstrating scalability with compute. DDIM [21] enables deterministic sampling; the <sup>??</sup> vs. <sup>??</sup><sub>0</sub> objective afects training stability [10, 13]; classifier-free guidance [11] trades diversity for conditioning fidelity; Fourier features [22] embed continuous conditioning signals. Crucially, the perception-distortion tradeof [5] proves that minimizing distortion (MSE/cosine similarity) and maximizing perceptual realism are fundamentally at odds. FID [9] and KID [4] measure the latter via Inception feature statistics. This lens is standard in generative vision but rare in AV latent prediction, where cosine similarity and L2 remain the default metrics. We bridge this gap by applying distribution-based evaluation to driving world models.

## 3. Dataset and Features

We use the nuScenes v1.0-trainval dataset [6], comprising 850 driving scenes (approximately 20 s each) captured at 2 Hz keyframe rate across diverse urban environments in Boston and Singapore, totaling 33,552 keyframes with synchronized camera and CAN-bus data.

Splits. We partition by scene into 630 training / 70 validation / 150 test, ensuring no scene appears in multiple splits. All metrics are reported on the 150 held-out test scenes.

Actions. Ego-vehicle actions are extracted from CANbus data as 2D vectors $a _ { t } = ( { \mathrm { s t e e r } } _ { t } , { \mathrm { a c c e l } } _ { t } )$ , z-score normalized using training-set statistics only.

Image features and latent encoding. For the encoder benchmark (Section 5.1), each keyframe is encoded to a 384-d pooled vector; encoders with diferent output dimensions use a frozen random orthogonal projection to 384- d, preserving distances without learnable parameters. V-JEPA2 (rep64 variant) ingests 16-frame clips via the full video transformer [3], while the rep1 variant uses only the spatial encoder on a single frame.

For the world model (Sections 5.3–5.4), we encode CAM FRONT frames at 256×256 resolution with the frozen Stable-Difusion VAE [19] to 32×32×4 latent grids (scaling factor 0.18215). Latents are patchified with patch size 4 to produce 8×8=64 spatial tokens of dimension 64 each. Actions are embedded with learned Fourier features [22] (64 frequencies per dimension). Figure 10 (Appendix) shows representative nuScenes frames and their VAE reconstructions.

Multi-horizon targets. Prediction targets span horizons $H \in \{ 4 , 8 , 1 6 \}$ (2, 4, 8 s at 2 Hz). Each example comprises a current latent $z _ { t }$ , action sequence $\{ a _ { t + 1 } , \ldots , a _ { t + H } \}$ , and future latents $\left\{ z _ { t + 1 } , \dots , z _ { t + H } \right\}$ . Evaluation uses scene-level aggregation: predictions within each test scene are averaged before computing final metrics, preventing false independence from overlapping windows.

Table 1: Encoder benchmark on 150 nuScenes test scenes (3 seeds each). RMSE is lower-better. V-JEPA2 (rep64, 16- frame temporal context) substantially outperforms all singleframe encoders.

<table><tr><td>Encoder</td><td>Steer RMSE</td><td>Accel RMSE</td></tr><tr><td>V-JEPA2 rep64</td><td> $\mathbf{0.058}_{\pm .012}$ </td><td> $\mathbf{0.055}_{\pm .004}$ </td></tr><tr><td>V-JEPA2 rep1</td><td> $0.097_{\pm .019}$ </td><td> $0.059_{\pm .004}$ </td></tr><tr><td>DINOv2-S/14</td><td> $0.104_{\pm .017}$ </td><td> $0.072_{\pm .004}$ </td></tr><tr><td>CLIP ViT-B/32</td><td> $0.117_{\pm .019}$ </td><td> $0.067_{\pm .004}$ </td></tr><tr><td>ViT-S/16</td><td> $0.121_{\pm .019}$ </td><td> $0.071_{\pm .004}$ </td></tr><tr><td>VQ-VAE Tracker</td><td> $0.126_{\pm .021}$ </td><td> $0.063_{\pm .005}$ </td></tr></table>

## 4. Methods

What is ours versus starter code. We adopt the DiT formulation [16] (adaLN-Zero conditioning) and the frozen SD-VAE [19]. FID/KID are computed via torchmetrics. All data pipeline, model architecture, training, evaluation, and analysis code is our own.

## 4.1. Encoder Benchmark

We evaluate six frozen encoders spanning four representation families (Table 1): ViT-S/16 [7] (supervised ImageNet-1k), DINOv2-S/14 [15] (self-distilled, no labels), CLIP-B/32 [18] (vision-language contrastive), V-JEPA2 in single-frame (rep1) and 16-frame-clip (rep64) configurations [3], and VQ-VAE Tracker [8] (reconstruction-oriented codebook). A shared 2-layer MLP probe (384→256 with GELU→2) predicts steering and acceleration from each encoder’s frozen embeddings (Adam, lr $1 0 ^ { - 3 }$ , batch 256, 50 epochs, 3 seeds). We report per-scene steering and acceleration RMSE on 150 test scenes, with 95% confidence intervals via bootstrap.

## 4.2. Latent DiT World-Action Model

Architecture. Our AnchoredVAEDiT comprises 4 transformer blocks with 4 attention heads and model dimension 256 (∼5.4 M parameters). Each block uses adaLN-Zero conditioning [16]: given conditioning vector <sup>??</sup>,

$$
\gamma , \beta , \alpha = \operatorname{MLP} (c), \quad h = \alpha \odot \operatorname{Attn} (\gamma \cdot \operatorname{LN} (x) + \beta),\tag{1}
$$

with gate <sup>??</sup> initialized to zero so each block starts as an identity function. The model receives <sup>??</sup> input tokens (noisy for difusion, or the present embedding replicated for direct regression) and predicts the clean future sequence $\widehat { z } _ { 0 } = \{ z _ { t + 1 } , \ldots , z _ { t + H } \}$ . Conditioning <sup>??</sup> sums three signals: a sinusoidal timestep embedding, the pooled present latent $z _ { t }$ (projected from patch dim to model dim), and the per-token Fourier action embedding.

Fourier action embedding. Each action pair $\begin{array} { r l } { a _ { k } } & { { } = } \end{array}$ (steer??<sup>,</sup> accel??) is embedded via learned Fourier features [22]:

$$
\operatorname{FourierEmbed} (a) = \left[ \sin (2 \pi f _ {j} a), \cos (2 \pi f _ {j} a) \right] _ {j = 1} ^ {N _ {f}}\tag{2}
$$

with $N _ { f } { = } 6 4$ learned frequencies per dimension, producing per-token embeddings $( B , H , D )$ that vary across the horizon, so that adaLN modulation difers at each prediction step.

Residual anchoring. Rather than predicting absolute future latents, the model predicts a residual:

$$
\hat {z} _ {t + k} = z _ {t} + \Delta_ {k} (z _ {t}, \{a _ {t + i} \}, \tau),\tag{3}
$$

where the present $z _ { t }$ is broadcast across all <sup>??</sup> horizon positions. This anchoring stabilizes early training and ensures the model degrades gracefully to copying the present rather than to random noise.

Difusion objective and sampling. The forward process adds Gaussian noise following a cosine schedule [13] with $T { = } 1 0 0 0$ timesteps: $q ( \tilde { z } _ { \tau } | z _ { 0 } ) = N \big ( \sqrt { \bar { \alpha } _ { \tau } } z _ { 0 } , ( 1 { - } \bar { \alpha } _ { \tau } ) I \big )$ . We train with the <sup>??</sup><sub>0</sub>-prediction objective:

$$
\mathcal {L} _ {\mathrm{diff}} = \mathbb {E} _ {\tau , \epsilon} \left\| \hat {z} _ {0} (\tilde {z} _ {\tau}, c, \tau) - z _ {0} \right\| _ {2} ^ {2},\tag{4}
$$

which is critical: our diagnosis shows that <sup>??</sup>-prediction causes collapse in compact latent spaces, while <sup>??</sup><sub>0</sub> recovers 88.5% of the performance gap (Section 5.2). We apply classifier-free guidance [11] via action dropout $\scriptstyle ( p = 0 . 1 )$ zeroing the action embedding with probability <sup>??</sup> during training. At inference, DDIM [21] sampling with 50 deterministic steps refines predictions from pure Gaussian noise.

Direct regression baseline. The same architecture at <sup>??</sup>=0 (no noise, single forward pass) serves as a strong deterministic baseline. A matched MLP baseline $( 3 8 4 \substack { \longrightarrow } 5 1 2 \substack { \longrightarrow } 3 8 4$ , GELU, residual output) provides a second reference.

## 4.3. Distribution Metrics and Calibration

Standard latent-prediction work evaluates with distortion metrics that measure per-sample closeness to ground truth. We additionally report distribution metrics that compare the decoded-frame distribution to the real-frame distribution:

FID [9] compares Inception-v3 feature statistics:

$$
\mathrm{FID} = \left\| \mu_ {r} - \mu_ {g} \right\| ^ {2} + \mathrm{Tr} \big (\Sigma_ {r} + \Sigma_ {g} - 2 (\Sigma_ {r} \Sigma_ {g}) ^ {1 / 2} \big).\tag{5}
$$

KID [4], our primary metric (robust at small <sup>??</sup>), is the squared MMD with polynomial kernel $\begin{array} { r } { k ( x , y ) = ( \frac { 1 } { d } \langle x , y \rangle + } \end{array}$ $1 ) ^ { 3 }$ . We report KID mean and standard deviation from torchmetrics, the latter serving as an error bar.

Because the VAE encoder/predictor induces a small perchannel ofset in the predicted latents, we estimate a deployable calibration (per-channel mean and scale shift) on the training split only and apply it at test time. This is distinct from any post-hoc use of test statistics and makes the FID/KID improvement deployable in a production system.

## 4.4. Motion Fidelity and the Jump Model

To probe coherent scene motion rather than texture churn, we decode all 16 predicted steps and decompose each consecutive-frame diference into:

• Low-frequency: Gaussian-blurred $( \sigma { = } 8 ,$ kernel 31) consecutive L2, coherent scene structure change.

• High-frequency: residual (original minus blurred) consecutive L2, measuring texture variation.

We also compute an image-plane displacement via horizontal and vertical profile cross-correlation, reporting the magnitude ratio and direction correlation versus ground truth. All metrics are evaluated on held-out test scenes.

Chain-anchor jump model. The single-pass model anchors every future token on the same present $z _ { t }$ (Eq. 3), which biases it toward re-rendering the current scene rather than accumulating ego-motion. We test a reparameterization: a compact (1.7 M-parameter, $n _ { \mathrm { b l o c k s } } = 2 ,$ dim 192) network predicts a single Δ<sup>??</sup>=4 transition $z _ { t }  z _ { t + 4 }$ , conditioned on the four intervening actions (Fourier-embedded and mean-pooled). At inference, this model is applied as a 4-step open-loop chain, re-anchoring on its own output at each step:

$$
z _ {t + 4 j} = f _ {\theta} \big (z _ {t + 4 (j - 1)}, \bar {a} _ {t + 4 (j - 1): t + 4 j} \big), j = 1, \ldots , 4\tag{6}
$$

reaching ${ z } _ { t + 1 6 }$ in four sequential transitions. Training uses teacher-forced ground-truth anchors; open-loop chain with the model’s predictions is the actual test of motion fidelity.

## 5. Experiments

## 5.1. Encoder Benchmark

V-JEPA2 rep64 achieves steering RMSE 0.058, a 40% reduction versus V-JEPA2 rep1 (0.097), the next-best encoder (Table 1, Figure 2). Scene-level paired <sup>??</sup>-tests with Bonferroni correction confirm significance against every alternative $( p _ { \mathrm { b o n f } } { < } 1 0 ^ { - 3 }$ , Cohen’s <sup>??></sup>0<sup>.</sup>68).

The controlled temporal ablation (rep64 vs. rep1, same architecture and checkpoint family, difering only in whether the video encoder processes 16 frames or 1) isolates temporal context as the source of improvement: frame-to-frame ego-motion patterns and lane curvature dynamics are invisible to single-frame encoders.

![](images/e788be04d13f199619dc235cacc956472303511fbe839d801b01b8ffa1eb3ac6.jpg)  
Figure 2: Steering RMSE across six frozen encoders (150 test scenes, 3 seeds, bootstrap CI). V-JEPA2 rep64 (temporal context from the full video encoder) achieves 40% lower error than the best single-frame encoder, demonstrating that temporal video representations capture ego-motion dynamics invisible to static frames.

Acceleration RMSE gaps are smaller (0.055 vs. 0.059); acceleration is more predictable from a single frame.

Among single-frame encoders, the self-supervised methods (DINOv2, V-JEPA2 rep1) outperform supervised ViT-S/16 and language-aligned CLIP on steering, suggesting that self-supervised features capture richer geometric structure. VQ-VAE Tracker ranks last, indicating that features optimized for image reconstruction encode appearance rather than the dynamics relevant to action prediction.

## 5.2. When Does a DiT Help? (Diagnosis)

In compact pooled latents (384-d), the DiT initially underperforms an MLP-residual baseline. A controlled diagnostic chain (Figure 3) tests four hypotheses:

• H1 (capacity): rejected. DiT-direct (no difusion) matches MLP, so the architecture is not the bottleneck.

• H2 (objective): confirmed. Switching from <sup>??</sup>- to <sup>??</sup><sub>0</sub>- prediction recovers 88.5% of the gap (the <sup>??</sup> objective collapses to near-copy in this regime).

• H3 (horizon): rejected. Longer horizons do not favor DiT; the 2 Hz posterior is near-unimodal when conditioned on logged actions.

• H4 (action-seq): partially confirmed. Per-token action-sequence conditioning benefits DiT more than MLP (interaction +0<sup>.</sup>007 to +0<sup>.</sup>020 CosSim on three encoders), confirming that self-attention exploits perstep temporal structure.

Restoring spatial tokens (Section 4.2) with residual anchoring, the DiT architecture beats matchedparameter MLPs on both ViT $( + 0 . 0 2 0 { \scriptstyle \pm 0 . 0 0 2 } )$ and DINOv2 $( + 0 . 0 2 3 { \scriptstyle \pm 0 . 0 0 2 } )$ at 12 M parameters, 3 seeds. This yields four ingredients, all necessary and jointly suficient: spatial tokens, the <sup>??</sup><sub>0</sub> objective, residual anchoring, and sampling matched to target uncertainty.

![](images/1b448b6c4ce6d7666715e62c1329af5cb21aac68873caac39c0cdc7d82214d3a.jpg)  
Figure 3: Diagnostic chain: <sup>??</sup>-prediction collapses (leftmost bars); switching to <sup>??</sup><sub>0</sub> recovers 88.5% of the MLP gap; adding spatial tokens lets the DiT architecture match or beat the MLP at equivalent parameters.

## 5.3. Perception-Distortion Frontier

In the SD-VAE pipeline, appearance ambiguity under point losses becomes pronounced, and the two model classes separate cleanly. The direct regressor wins every distortion metric (CosSim 0<sup>.</sup>471 vs. 0<sup>.</sup>260; higher SSIM) by predicting the blurry conditional mean. The difusion model wins every distribution metric. On KID, our primary small-<sup>??</sup>- robust measure, difusion with the deployable train calibration attains 0<sup>.</sup>078 versus 0<sup>.</sup>375 for direct regression, a 4<sup>.</sup>8× improvement. FID tells the same story (162<sup>.</sup>5 vs. 370<sup>.</sup>8). Across 3 independent seeds, KID is 0<sup>.</sup>076±0<sup>.</sup>005 (Table 2, Figure 4).

This is precisely the perception-distortion tradeof [5]: distortion metrics, standard in latent-prediction work, systematically reward the wrong thing for world models. Figure 10 (Appendix) makes the tradeof visual: the direct row is a sharpness-collapsed blur, while the difusion row renders a recognizable street scene with plausible vehicles and road markings.

Table 2: Distribution vs. distortion at <sup>??</sup>+16 (held-out test). KID/FID lower is better; CosSim higher is better. Difusion uses the deployable train-derived calibration. Interp = latent interpolation (<sup>??</sup>=0<sup>.</sup>5) between direct and difusion.

<table><tr><td>Model</td><td>KID↓</td><td>FID↓</td><td>CosSim↑</td></tr><tr><td>Direct (regression)</td><td>0.375</td><td>370.8</td><td>0.471</td></tr><tr><td>Diffusion (raw)</td><td>0.294</td><td>341.9</td><td>0.233</td></tr><tr><td>Interp ( $\alpha = .5$ )</td><td>0.084</td><td>166.6</td><td>0.316</td></tr><tr><td>Diffusion (calib.)</td><td>0.078</td><td>162.5</td><td>0.260</td></tr><tr><td>VAE-GT ceiling</td><td> $\approx 0$ </td><td> $\approx 0$ </td><td>1.000</td></tr></table>

![](images/c51cf499aba9ea6f6f4e334a0ec521f4a0cb2d251f8bccd7a45e61c79c3ad0da.jpg)

![](images/b7e336aec245d2d80d7d4074de95a94c1cfb245d100601be3704bde71bea93fe.jpg)  
Figure 4: FID/KID at two horizons (<sup>??</sup>+4 and $t + 1 6 ,$ 3 seeds, 600 held-out windows). The difusion model (traincalibrated) is far closer to the real frame distribution than direct regression, approaching the VAE-GT ceiling. Error bars are seed standard deviation.

A deployable frontier. The operating points trace an empirical distortion-perception curve (Figure 5): moving from direct regression through latent interpolation to calibrated difusion trades distortion for perceptual realism.

![](images/eceb8cc249ba34f64b53dbb6d52f9dd6e66004c30c2e50d5d0af65d2ffefb0f6.jpg)  
Figure 5: Empirical distortion-perception frontier (<sup>??</sup>+16, 150 test windows). Distortion (CosSim to GT) on the <sup>??</sup>- axis, distribution quality (KID, lower is better) on the <sup>??</sup>- axis. The deployable difusion model occupies the highrealism regime; latent interpolation provides an intermediate operating point. Error bars: KID standard deviation.

The calibration that unlocks this KID advantage is estimated on training data and applied at test time; it recovers a post-hoc oracle calibration (KID 0<sup>.</sup>078 vs. 0<sup>.</sup>086), so the advantage is deployable rather than a test-time artifact. A 2- point capacity probe (3.0 M vs. 5.4 M, 1 seed each) shows the larger model is better on every difusion point (KID 0<sup>.</sup>078 vs. 0<sup>.</sup>089, FID 162<sup>.</sup>5 vs. 172<sup>.</sup>8), indicating the frontier shifts favorably with scale. This probe is preliminary (2 points, 1 seed) but the direction is clean and consistent.

![](images/7fb0a91f2466fea59482529e3590cd3cdea1b387627045170d556f256584a079.jpg)  
Figure 6: Motion fidelity diagnostic (16-step, low/highfrequency decomposition). Difusion reproduces texture (0<sup>.</sup>98× GT) but little coherent motion (0<sup>.</sup>44×); the regression mean captures more scene-level motion (0<sup>.</sup>56×). The compact jump model (Section 5.4) recovers motion direction (0<sup>.</sup>48), exceeding the larger single-pass baseline (0<sup>.</sup>41).

## 5.4. Motion: Diagnosis and a Compact Fix

We provide a careful accounting of temporal fidelity. A single difusion rollout reproduces per-frame texture (highfrequency fraction 0<sup>.</sup>98× GT) but shows limited coherent scene motion: low-frequency motion fraction is only 0<sup>.</sup>44× GT (difusion) compared to 0<sup>.</sup>56× (the blurry direct model), and image-plane displacement is near zero for difusion (Figure 6). A motion-targeted fine-tune (adding a temporaldiference loss $\| \Delta z _ { \mathrm { p r e d } } - \Delta z _ { \mathrm { G T } } \| ^ { 2 }$ for 30 epochs) did not improve these numbers, indicating a structural rather than a loss-surface issue.

Diagnosis. We trace this to the shared-present anchoring (Eq. 3): every future token is computed as a delta from the same $z _ { t }$ . At 2 Hz driving, this biases the model toward re-rendering the current layout with varied texture rather than accumulating the ego-motion that would shift the scene forward.

![](images/576c3f91e861c68d4d08644731132dfe954e6164ebe06af5979843dd1004f255.jpg)  
Figure 7: Motion decomposition on a held-out nuScenes scene. Each road frame is overlaid with low-frequency change (hot colormap: coherent scene motion from egodriving) or high-frequency change (cool colormap: texture variation) between consecutive decoded frames (<sup>??</sup>→<sup>??</sup>+4). Top: ground truth; bottom: the compact jump model. The overlay shows that the model captures the spatial distribution of scene-level motion, even though decoded frames are blurry.

The jump model recovers motion direction. Reparameterizing prediction as a Δ<sup>??</sup>=4 jump with per-step reanchoring (Eq. 6), a 1.7 M-parameter model (3× smaller than the 5.4 M baseline) predicts forward motion whose image-plane direction correlates with ground truth at 0<sup>.</sup>48 on 30 held-out test scenes (open-loop, own anchors), exceeding the larger single-pass model (0<sup>.</sup>41) and capturing the full low-frequency motion magnitude (1<sup>.</sup>02× GT). This supports the hypothesis that limited motion was an objectiveand-anchoring choice, not a capacity limitation.

Figure 8 shows the chain-anchor jump rollout on a held-out nuScenes scene: the 1.7 M model predicts $z _ { t + 4 } , z _ { t + 8 } , z _ { t + 1 2 } , z _ { t + 1 6 }$ by re-anchoring on its own output at each step. Decoded predictions (middle row) show recognizable scene structure at early steps, with regression blur compounding visibly toward later steps; the groundtruth row (bottom) provides context. Figure 7 visualizes the motion decomposition, overlaying low-frequency (coherent scene motion) and high-frequency (texture) changes on decoded frames; the jump model captures the spatial pattern of scene-level motion even though decoded frames remain blurry. The jump predictions are coarse (regression blur persists at this scale); recognizable high-fidelity appearance over 8 seconds remains a question of scale and stronger temporal supervision, which we leave to future work.

![](images/66c8d99445e3827b47780a529bf160a9d9d79a2c9a191056bd738ef27125de6b.jpg)  
Figure 8: Chain-anchor jump world model (4-step open-loop rollout on a held-out nuScenes scene). Top (pink): the 1.7 M Jump DiT predicts Δ<sup>??</sup>=4 transitions, re-anchoring on its own output at each step. Middle (blue): decoded predictions at $t + 0 , t + 4 , t + 8 , t + 1 2 , t + 1 6$ . Bottom (gray): ground-truth future frames. Scene structure is recognizable at early steps; regression blur compounds visibly toward $t + 1 6$ , illustrating both the motion recovery and the scale limitation.

## 5.5. Action Controllability

A world model must use actions, not merely render plausible scenes. We sweep the steering input across its training distribution range (5th to 95th percentile) with fixed difusion noise and measure the induced horizontal scene displacement at <sup>??</sup>+15 on 40 held-out windows.

The difusion model is monotonically controllable: Spearman $\rho ( \mathrm { s t e e r } , \mathrm { s h i f t } ) = + 0 . 8 1$ with 100% sign-correct on the 18/40 scenes where shift exceeds a detection threshold (Figure 9). The direct regression model is uncorrelated $( \rho { = } { - } 0 . 1 8 , 3 9 / 4 0$ valid).

Steering left versus right produces visibly diferent predicted futures, providing rigorous evidence for a “world-action” model. This controllability is a planningrelevant property: a non-circular inverse-control probe (predicting the held-out steering value that produced a given future, not evaluated on training data) finds the difusion model recovers target steers at 0<sup>.</sup>67× chance error, while the direct model performs worse than random (1<sup>.</sup>24×).

![](images/f6fb3c41170a31075ed9f5c972bdefbab2f909d54d32c4308a4acfe31da1d78f.jpg)  
Figure 9: Action controllability: steering input vs. induced scene displacement at <sup>??</sup>+15 (fixed noise, 40 heldout scenes). The difusion world model is monotonically steerable $( \rho { = } 0 . 8 1 )$ ; the regression baseline is uncorrelated $( \rho { = } \substack { - } 0 . 1 8 )$

## 5.6. Failure Modes, Memorization, and Overfitting

Failure modes. Difusion samples exhibit a mild perchannel color tint (a systematic ofset removed by the deployable calibration) and slight over-sharpening (high-frequency energy ∼2× GT standard deviation at small scale). These are typical small-model artifacts; neither prevents scene recognition. The direct model’s primary failure mode is diagnostic blur (see Figure 10 in the Appendix): regression to the conditional mean collapses fine structure, rendering vehicles and lane markings unrecognizable. The jump model’s coarse predictions (blurry at <sup>??</sup>+4, increasingly so through the 4-step chain) represent a related limitation: recognizable multi-second predictions require stronger generative objectives or higher capacity than explored here.

Memorization. FID and KID are computed against heldout test frames decoded from ground-truth latents; the model never sees test scenes during training. We additionally verify that nearest-neighbor distances from generated samples to the training manifold do not indicate copying.

Overfitting. Models are compact (1.6–5.4 M for the world model, 1.7 M for the jump experiment) relative to the 24,930-frame training set. All results use scene-level held-out evaluation with 3 random seeds; reported gaps consistently exceed seed-level variance $( \mathrm { K I D _ { s t d } } { = } 0 . 0 0 5$ across seeds). EMA (decay 0.999) provides implicit regularization. The chief threat to validity is scale: our controlled comparisons are meaningful at compact scale and single front camera (2 Hz), but do not address whether the same factors hold at GAIA-1 or Cosmos scale, which we state as a limitation.

## 6. Discussion

Our experiments reveal a consistent theme: the choice of evaluation metric shapes which model appears superior, and this choice has practical consequences for AV world model development.

The metric matters more than the architecture. In pooled latent spaces, the DiT architecture is not the bottleneck (H1 rejected); the prediction objective and spatial structure are. In VAE latent space, the direct regressor appears to “win” on every standard metric (CosSim, SSIM, L2) by collapsing to the conditional mean. Only when we measure distribution realism via FID and KID does the difusion model’s 4<sup>.</sup>8× advantage emerge. This suggests that prior AV latent-prediction work that evaluates exclusively with distortion metrics may underestimate the quality of generative approaches. We recommend that future AV world model evaluations report both distortion and distribution metrics, following the perception-distortion framework of Blau and Michaeli [5].

Controllability as a diferentiator. The difusion model’s monotonic steering controllability (<sup>??</sup>=0<sup>.</sup>81) provides a capability the regression model lacks entirely $( \rho { = } \substack { - } 0 . 1 8 )$ . This is not merely a qualitative observation: the inverse-control probe shows the difusion model can recover held-out steering commands from predicted futures (0<sup>.</sup>67× chance error), suggesting the learned latent dynamics encode action semantics in a way that could support downstream planning. At larger scale, this property could bridge world models and policy learning.

Motion as an anchoring problem. Our motion analysis reveals that the single-pass model’s static behavior is not a capacity limitation but a consequence of the shared-present anchor design. The compact jump model (1.7 M parameters,

3× smaller) recovers motion direction by re-anchoring on its own predictions, confirming that the architectural recipe is sound and the bottleneck is the training-inference alignment. This finding provides a concrete path forward: perstep re-anchoring combined with stronger temporal supervision (e.g., scheduled sampling or perceptual losses) should enable recognizable multi-second predictions as scale increases.

Compact scale as a feature. While large-scale systems like GAIA-1 [12] (5 B parameters) and Cosmos [14] demonstrate impressive generation quality, our compactscale study (∼5 M parameters) ofers complementary value: controlled ablations that isolate individual design factors are tractable at this scale but prohibitively expensive at 5 B. The four ingredients we identify (spatial tokens, $x _ { 0 }$ objective, residual anchoring, sampling-matched-to-uncertainty) and the deployable calibration strategy are design principles that transfer to larger systems, and the positive scaling probe (Section 5.3) provides initial evidence that they do.

## 7. Conclusion

We built a compact, action-conditioned DiT world model for AV scene prediction and, in the process, turned two obstacles into results.

First, distortion metrics that reward the blurry conditional mean masked the difusion model’s quality. By measuring distribution realism via FID and KID, we revealed a clean perception-distortion frontier where difusion is 4<sup>.</sup>8× better on KID, made deployable through a train-derived calibration. This is a metric lesson for AV latent prediction: world models should be evaluated with distribution metrics, not distortion alone.

Second, single-pass rollouts produced sharp but temporally limited predictions. We diagnosed the shared-anchor cause and showed that a compact jump model (3× smaller, re-anchored per step) recovers forward-motion direction on held-out scenes, exceeding the larger baseline. The model is also action-controllable (<sup>??</sup>=0<sup>.</sup>81), providing the planningrelevant property that a regression baseline lacks.

Together these establish a small but controllable AV world model with a characterized realism frontier and a validated route to coarse motion.

Future work. The frontier and the jump result both point the same way: scale (capacity, data, frame rate) and stronger temporal supervision (e.g., perceptual losses, autoregressive scheduled sampling) to convert coarse motion direction into recognizable high-fidelity appearance. Closed-loop evaluation with predicted (rather than logged) actions and multicamera setups are natural extensions. Our controlled findings at compact scale provide a concrete recipe for the next iteration: the $x _ { 0 }$ objective, residual anchoring, distribution metrics, deployable calibration, and the jump reparameterization.

## Contributions and Acknowledgements

Ruslan Sharifullin: project direction and experiment planning, six-encoder benchmark design and probe training, DiT world model architecture, VAE encode-predict-decode pipeline, difusion training and DDIM sampling, perceptiondistortion frontier analysis, FID/KID evaluation framework, action controllability experiments, motion fidelity diagnostics, chain-anchor jump model, cloud compute infrastructure, report writing and editing.

Benjamin Jiang: nuScenes data pipeline (frame/clip dataset, scene-level splits, CAN-bus extraction), RMSE evaluation harness with bootstrap CIs, perturbation analysis pipeline, environmental robustness analysis (night/rain/day bucketing), encoder attribution visualization (GradCAM), RMSE heatmaps and CosSim bar charts, report editing.

Kai Xi Chew: behavioral cloning baseline implementation and training, per-horizon CosSim and DeltaCosSim evaluation pipeline, embedding precomputation infrastructure, report editing.

## Source Code

Source code, trained checkpoints and evaluation artifacts are available at https://github.com/dlcv-team/ latent-world-models-av.

## References

[1] E. Alonso, A. Jelley, A. Kanervisto, and T. Pearce. DI-AMOND: Difusion for world modeling. arXiv preprint arXiv:2405.12399, 2024.

[2] A. Bardes et al. V-JEPA: Latent video prediction for visual representation learning. arXiv preprint arXiv:2404.08471, 2024.

[3] A. Bardes et al. V-JEPA 2: Self-supervised video models enable understanding, prediction and planning. arXiv preprint arXiv:2506.09985, 2025.

[4] M. Binkowski, D. J. Sutherland, M. Arbel, and A. Gretton.´ Demystifying MMD GANs. In International Conference on Learning Representations (ICLR), 2018.

[5] Y. Blau and T. Michaeli. The perception-distortion tradeof. In Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR), pages 6228–6237, 2018.

[6] H. Caesar et al. nuScenes: A multimodal dataset for autonomous driving. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR), 2020.

[7] A. Dosovitskiy et al. An image is worth 16x16 words: Transformers for image recognition at scale. In International Conference on Learning Representations (ICLR), 2021.

[8] P. Esser, R. Rombach, and B. Ommer. Taming transformers for high-resolution image synthesis. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR), 2021.

[9] M. Heusel, H. Ramsauer, T. Unterthiner, B. Nessler, and S. Hochreiter. GANs trained by a two time-scale update rule converge to a local Nash equilibrium. In Advances in Neural Information Processing Systems (NeurIPS), 2017.

[10] J. Ho, A. Jain, and P. Abbeel. Denoising difusion probabilistic models. Advances in Neural Information Processing Systems, 2020.

[11] J. Ho and T. Salimans. Classifier-free difusion guidance. arXiv preprint arXiv:2207.12598, 2022.

[12] A. Hu et al. GAIA-1: A generative world model with integrated action understanding. arXiv preprint arXiv:2309.17080, 2023.

[13] A. Q. Nichol and P. Dhariwal. Improved denoising difusion probabilistic models. International Conference on Machine Learning (ICML), 2021.

[14] NVIDIA. Cosmos world foundation model platform for physical ai. arXiv preprint arXiv:2501.03575, 2024.

[15] M. Oquab et al. DINOv2: Learning robust visual features without supervision. Transactions on Machine Learning Research, 2024.

[16] W. Peebles and S. Xie. Scalable difusion models with transformers. In International Conference on Computer Vision (ICCV), 2023.

[17] A. Polyak et al. Movie gen: A cast of media foundation models. arXiv preprint arXiv:2410.13720, 2024.

[18] A. Radford et al. Learning transferable visual models from natural language supervision. In International Conference on Machine Learning (ICML), 2021.

[19] R. Rombach, A. Blattmann, D. Lorenz, P. Esser, and B. Ommer. High-resolution image synthesis with latent difusion models. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR), 2022.

[20] C. Shi, J. Xu, S. Shi, K. Sheng, B. Zhang, and L. Jiang. DriveWAM: Video generative priors enable scalable worldaction modeling for autonomous driving. arXiv preprint arXiv:2605.28544, 2026.

[21] J. Song, C. Meng, and S. Ermon. Denoising difusion implicit models. In International Conference on Learning Representations (ICLR), 2021.

[22] M. Tancik et al. Fourier features let networks learn high frequency functions in low dimensional domains. In Advances in Neural Information Processing Systems, 2020.

[23] C. Yang et al. Driveworld: 4d pre-trained scene understanding via world models for autonomous driving. arXiv preprint arXiv:2405.04390, 2024.

[24] M. Yang et al. UniSim: Learning interactive real-world simulators. arXiv preprint arXiv:2310.06114, 2023.

[25] X. Zhao et al. Drivedreamer: Towards real-world-driven world models for autonomous driving. arXiv preprint arXiv:2309.09777, 2024.

[26] J. Zheng et al. GenAD: Generalized predictive model for autonomous driving. arXiv preprint arXiv:2405.09349, 2024.

![](images/ca807b6c22798e847d3ab25c768e20aff59dc512c7607c33007a167289873e87.jpg)  
Figure 10: Qualitative VAE encode-predict-decode on a held-out nuScenes scene (<sup>??</sup>+0 through <sup>??</sup>+15). Row 1: camera RGB ground truth. Row 2: VAE-GT reconstruction (the appearance ceiling of this latent space). Row 3: DiT-direct prediction, whose blur is regression mean-collapse (not VAE tokenization loss). Row 4: DiT-difusion prediction (train-calibrated), which renders in-distribution frames with recognizable scene content. This side-by-side contrast is the perception-distortion tradeof made visual: the direct model minimizes distortion at the cost of sharpness, while the difusion model produces realistic appearance at the cost of per-pixel fidelity.